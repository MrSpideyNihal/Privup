"""Extract clean policy text from raw page content."""


class Extractor:
	"""Basic extractor placeholder."""

	def extract(self, raw_content: str) -> str:
		"""Return normalized placeholder content."""
		return raw_content.strip()
